/**
 * This module re-exports TabsProps, TabProps and TabsEvents component.
 * It serves as an entry point for easier importing in other parts of the application.
 */

import TabsProps from "./tabsProps";
import TabProps from "./tabProps";
import TabsEvents from "./tabsEvents";

/**
 * Export TabsProps, TabProps and TabsEvents component.
 */

export { TabsProps, TabProps, TabsEvents };
